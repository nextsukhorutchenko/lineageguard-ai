# Judging Map

Category: **Metadata-Aware Code Generation & Development**

The judging story is causal: DataHub evidence changes the migration decision and the generated
package. Fixture replay is the deterministic judging path; Live DataHub + OpenAI proof remains
separately governed by `docs/live-verification.md`.

The public deterministic replay is available at <https://lineageguard-ai-replay.onrender.com>.
Sanitized commit-bound acceptance evidence is recorded in
[`docs/public-deployment-verification.md`](public-deployment-verification.md); it records only the
verified public replay outcome and recovery boundary.

| Official criterion                            | Repository evidence                                                                                                     | Visible demo moment                                                                                                                                                                                                                  | Test or live check                                                                                                                                                      |
| --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Use of DataHub                                | `README.md`, `docs/architecture/agent-demo.md`, and `examples/002-nextjs-openai-agent-demo/rollout-plan.md`             | Resolve the golden DataHub asset and schema; show table and column lineage, ownership and Context Coverage; show four read-only operation summaries; connect 24 downstream / 11 column-confirmed / score 90 to `BLOCK_DIRECT_RENAME` | `pnpm test:integration` for the opt-in pinned live contract; `pnpm verify:offline` for certified replay                                                                 |
| Technical Execution                           | `src/agent/`, `src/datahub/`, `src/runs/`, `scripts/validate-submission-assets.ts`, and the four public artifacts       | Show the exact `analyze_rename_change` and `generate_migration_package` calls, normalized bounded evidence, deterministic validation, and the non-executable physical-name gate                                                      | `pnpm test:submission`, `pnpm submission:check`, `pnpm typecheck`, and `pnpm verify:offline`                                                                            |
| Originality                                   | `docs/architecture/agent-demo.md`, `docs/resources-and-attribution.md`, and `skills/lineageguard-schema-change-impact/` | Explain the context-first migration planner, separate Evidence Completeness from Context Coverage, and show the clean-room architecture                                                                                              | Submission validator clean-room/source checks plus repository secret scan                                                                                               |
| Real-World Usefulness                         | `examples/002-nextjs-openai-agent-demo/README.md` and the generated `rollout-plan.md`                                   | Show why a direct rename is blocked, how four review-ready artifacts support staged compatibility, and where human approval remains mandatory                                                                                        | Golden replay browser flow and deterministic 24/11/90 fixture assertions in `pnpm verify:offline`                                                                       |
| Submission Quality                            | `README.md`, `docs/demo-scenario.md`, `docs/submission-checklist.md`, and `docs/resources-and-attribution.md`           | Complete the seven-beat 2:55 story with LIVE/REPLAY evidence source, readable proof panels, and an explicit read-only/no-SQL close                                                                                                   | `pnpm submission:check`, `pnpm format:check`, `pnpm lint`, and repository-wide security scans                                                                           |
| Contribution candidate — bonus not yet earned | `skills/lineageguard-schema-change-impact/` and its pinned MCP reference/template                                       | Show the local read-only candidate only as an optional follow-on artifact                                                                                                                                                            | Pending: no upstream DataHub PR exists. Bonus remains unearned until a separately approved clean-fork PR passes the upstream repository's then-current mandatory checks |

The candidate row is not an official contribution claim. Local validation establishes only
repository-owned candidate readiness; it does not substitute for upstream review, CI, or merge.
