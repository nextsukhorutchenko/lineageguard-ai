# LineageGuard AI

LineageGuard AI is a CLI-first TypeScript project for deterministic DataHub impact analysis.

## Development

Use Node.js 22.23.1 and pnpm 10.10.0, then run:

```sh
pnpm install
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```
